const { test } = require('node:test')

test('E2E browser flow skeleton: login admin, laporan range, and public absensi camera flow', {
  todo: 'Add Playwright once browser test dependencies are introduced.',
}, () => {})
